import chalk from 'chalk';

const clr = {
    arch: chalk.bold.cyan,
    white: chalk.white,
    gray: chalk.gray
};

export const archlogo = `
${clr.arch('          /\\')}
${clr.arch('         /  \\')}
${clr.arch('        / /\\ \\')}
${clr.arch('       / /  \\ \\')}
${clr.arch('      / /    \\ \\')}
${clr.arch('     / /  /\\  \\ \\')}
${clr.arch('    / /__/  \\__\\ \\')}
${clr.arch('   /____________  \\')}
${clr.arch('                \\__\\')}
`;